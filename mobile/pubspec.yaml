name: points_master
description: Points Master Game
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_riverpod: ^2.4.0
  dio: ^5.3.0
  socket_io_client: ^2.0.3
  shared_preferences: ^2.2.0
  # firebase_auth: ^4.14.0
  # google_sign_in: ^6.1.6
  # sign_in_with_apple: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
